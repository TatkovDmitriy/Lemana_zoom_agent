[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/zoom-bot/Dockerfile"
buildCommand = ""

[deploy]
startCommand = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 30
